import React from 'react';
import { PersonIcon } from '@primer/octicons-react';

const UserInfo = () => (
  <div className="user-info text-right">
    <PersonIcon /> Name LastName
  </div>
);

export default UserInfo;
